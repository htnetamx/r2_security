import Role from '../repositories/role'
export const createRoles = async() =>  {
    /*try{
        const count = await Role.estimatedDocumentCount();
        if(count>0) return;
            const values=await Promise.all([
                new Role({role:'EndClient'}).save(),
                new Role({role:'Store'}).save(),
                new Role({role:'Admin'}).save(),
                new Role({role:'AllMight'}).save(),
            ]);
    }catch(error){
        console.log(error);
    }*/
};